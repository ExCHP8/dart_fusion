part of '../dart_fusion.dart';

/// Define [JSON] Type as a shortcut for [Map<String, dynamic>].
typedef JSON = Map<String, dynamic>;

/// A typedef alias for [ResponseAssertion].
///
/// The [Assert] typedef creates an alias for the [ResponseAssertion] class,
/// allowing the use of [Assert] as an alternative name for [ResponseAssertion].
typedef Assert = ResponseAssertion;
